#if !defined SL_LE_gui_base_slCCocoaScreenImpl_hp
#define SL_LE_gui_base_slCCocoaScreenImpl_hp

#include <le/core/slCString.h>
#include <le/gui/slTypes.h>
#include <le/gui/auxiliary/slCGuiCocoaApplication.h>

namespace sokira
{
	namespace le
	{

class CScreen;

class CCocoaScreenImpl
{
	public:
		CCocoaScreenImpl(bool fullScreen, const CString& title, const CRectangle& rect);

		void screenWillBeAddedToApplication(CScreen* screen, CGuiCocoaApplication* app);
		void screenWasAddedToApplication(CScreen* screen, CGuiCocoaApplication* app);

		void screenWillBeRemovedFromApplication(CScreen* screen, CGuiCocoaApplication* app);
		void screenWasRemovedFromApplication(CScreen* screen, CGuiCocoaApplication* app);

		Bool inLiveResize() const;

		CSize2D size() const;
		void setSize(const CSize2D& size);

		void setNeedsRedraw();

		CString title() const;

	private:
		Bool mFullScreen;
		CString mTitle;
		CRectangle mRect;
		void* mWindow;
};

	} // namespace le
} // namespace sokira

#endif // not defined SL_LE_gui_base_slCCocoaScreenImpl_hp
